services:
  tienda-insumospepira:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tienda-insumospepira
    # ports:
    #   - "5174:80"
    restart: unless-stopped
    networks:
      - redPrincipal

networks:
  redPrincipal:
    external: true