services:
  tienda-ferrseb:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tienda-ferrseb
    # ports:
    #   - "5174:80"
    restart: unless-stopped
    networks:
      - redPrincipal

networks:
  redPrincipal:
    external: true